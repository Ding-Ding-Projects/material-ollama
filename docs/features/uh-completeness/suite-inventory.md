# Local suite inventory

This is the hand-written contract for every local test/verification **suite area** the release
gate cares about — not a description of what happens to already be running, but a fixed list of
what MUST have a row, checked against the real repository by
[`scripts/check-suite-inventory.mjs`](../../../scripts/check-suite-inventory.mjs). A suite area
omitted from the table below fails that gate; a row whose `covered`/`partial` evidence does not
actually resolve on disk fails it too. There is no mechanism by which this document can silently
shrink to match whatever suites happen to exist — the checker's own required-area list is
hand-written and independent of this file's contents (see that script's `REQUIRED_AREA_IDS`).

**A `missing` (or `partial`) status is not a defect in this document; it is an honest report.**
Where no dedicated suite exists yet for an area at all, the row says so plainly, together with the
real reason and (where one exists) a pointer at the real, untested implementation. Where a suite
exists but does not reach the whole area — most commonly because the untested remainder talks to a
real external process (a real Ollama pull, a real spawned terminal window) that a unit test must
not trigger — the row stays `partial` and names exactly what is and is not covered. Inflating a row
to `covered` because *something* nearby has a test, or because the untested remainder was
deliberately left untested for a good reason, would make this document actively misleading, which
is worse than an honest gap.

## Status vocabulary

- **`covered`** — a real, committed suite exists for this area, its evidence resolves against the
  real filesystem (an exact test declaration or a real script), and it was run and observed passing
  this session.
- **`partial`** — real evidence exists and resolves, but it covers only part of the area (a narrow
  scenario, one branch of several, a rendering-safety slice of a bigger feature) or the underlying
  check is currently failing for reasons outside this lane's scope. The `Notes` column says exactly
  what is and is not covered.
- **`missing`** — no dedicated suite exists for this area at all. `Command` and `Evidence` both
  carry the literal placeholder `—`, and `Notes` carries the real reason (and, where one exists, the
  real untested implementation to point a future lane at).

## Inventory

| Area ID | Area | Command | Scope | Evidence | Status | Notes |
|---|---|---|---|---|---|---|
| unit | Unit (Go) | go test ./app/... | Pure-function and small-surface Go unit tests across every app/... package that has one — 9 packages as of this writing (format, logrotate, server, store, tools, types/not, ui, ui/buildinfo, updater). No real network, no real subprocess, no real GUI. | app/format/field_test.go::TestKebabCase | covered | Requires CGO_ENABLED=1 and this project's pinned Go toolchain plus the bundled MinGW C compiler on PATH (app/ui's mlxrunner cgo files fail to compile without it — see this project's own environment notes). Measured earlier this session (non-verbose, cache-hit): all 9 packages report ok, 0 visible failures. Re-measured later the same session with -v (real execution, not a cache hit): 113 --- PASS lines and 1 --- FAIL -- app/updater.TestIsInstallerRunning panics ("slice bounds out of range [:2432] with capacity 2048") inside IsProcRunning (app/updater/updater_windows.go:384), reproduced twice in direct succession. This is a real, environment-dependent capacity bug -- IsProcRunning enumerates every live Windows process into a fixed-size buffer and does not grow it when the live process count exceeds that buffer's capacity, which this long, tool-heavy session's own elevated process count was enough to trigger. It is unrelated to any file this evidence pass's allowed paths could touch (app/updater/updater_windows.go is Go source), so it is reported here rather than silently left for the next reader to rediscover as a mysterious flake. Grown in an earlier lane by app/ui/docker_test.go (Docker GPU capability probe against a fake docker CLI re-invoking this same test binary — cpu-only-with-reason on a non-WSL2 backend, docker-missing, image-not-pulled, --gpus-rejected, no-devices, and the gpu-available green case) and app/ui/release_test.go (GET /api/v1/release reports a dev build as version 0.0.0 with every dish/workflow field nil, per app/version.Version's own zero value). |
| integration | Integration | go test ./app/store/... -run TestChatDeletionWithCascade | Multi-layer round trips against a REAL sqlite database file on a real temp directory (not a mocked store layer) — schema creation, save, cascading foreign-key delete, and a follow-up query, all through the real driver. | app/store/database_test.go::TestChatDeletionWithCascade | covered | app/store/store_test.go::TestStore and the rest of database_test.go/migration_test.go are the same style. This is the closest thing this repo has to a dedicated integration tier; nothing spins up the real HTTP server AND the real store AND a real subprocess together in one test. |
| dom-interaction | DOM / interaction | npx vitest run | The UI's complete Vitest suite from app/ui/app — real jsdom rendering plus @testing-library/user-event keyboard/click interaction across 39 *.dom.test.tsx files, run alongside 11 Node-environment pure-function unit test files in the same command. | app/ui/app/src/components/md3/ConfirmDialog.dom.test.tsx::keeps the action disabled until the exact keyword is typed, then fires it on click | covered | Measured this session: 50 test files / 353 tests, all passing (up from 9 files / 59 tests). Grew across seven merged feature lanes covering, at minimum: the Settings screen (six real cards -- General, Language & Voice, Appearance, Advanced, Data & Privacy, School mode -- plus a real provenance/search suite in SettingsScreen.dom.test.tsx); the browser-style tab system (TabStrip docking/groups, TabGroupsPanel, tabBulkClose, CommandPalette); the Toolbox's real converter and authenticator (ConvertCategoryList, TotpAccountRow, qrEncoder's 9-case RFC/ISO-18004 suite); the Status screen (ChangelogCard, LocalHistoryCard, SupportTicketsCard, DimSum, AutomaticUpdatesCard, dateRange/changelogEntries/dimSumSurprise/exportUtils); the toy-lock system (Lockable, LockManager, UnlockLadder, and four uh/locks*.dom.test.tsx files covering RFC 6238 vectors, redacted history, and the unlock ladder's nonce/timing/budget rules); exports and bulk actions (ExportDialog/ExportPreview/exportFormats/useExport/yamlStringify/openInEditor, and BulkActionBar/BulkActionPreviewDialog/BulkSelectableList/BulkSelectAllHeader/useBulkActionRunner/useBulkSelection); and four accessibility primitives (a11yFocusTrap/a11yLayoutAudit/a11yRovingTabindex/a11yScrollRegion). |
| model-catalog | Model catalog | go test ./app/ui/... -run TestRunCatalogRefresh_OneFailingTagPageYieldsPartialVerdict | Real end-to-end exercise of runCatalogRefresh against a local httptest.Server standing in for registry.ollama.ai/ollama.com (a RoundTripper rewrites only the request scheme/host) — the catalog API probe, the HTML index fallback, the tags/list probe, and one real 404 on a repository's tag page, proving the completeness verdict comes out partial (never complete) with the exact reason string. | app/ui/catalog_test.go::TestRunCatalogRefresh_OneFailingTagPageYieldsPartialVerdict | covered | Added in this lane. Only one scenario is exercised (one real 404 among several repositories); the deadline-truncation and repository-cap branches of the same verdict switch remain untested. |
| hardware-fit | Hardware fit | go test ./app/ui/... -run TestComputeFitVerdict_NilModelBytesYieldsUnknown | computeFitVerdict's ordered rules — nil model bytes short-circuits to unknown (never runs-well) regardless of how confidently RAM/VRAM are known, both-RAM-and-VRAM-unknown also short-circuits, and a contrasting green case proves the function can genuinely reach runs-well. | app/ui/hardware_test.go::TestComputeFitVerdict_NilModelBytesYieldsUnknown | covered | Added in this lane (3 test functions). The runs-with-limits (rules 3/4) and unlikely (rule 5) branches, and the real hardware-detection path (buildHardwareResponse/server.GetInferenceInfo scraping), remain untested — see the ollama-manager row. |
| pull-queue | Batch pull queue | go test ./app/ui/... -run TestModelsManagerLoadLocked_DownloadingResetToQueuedOnRecovery | The batch model-pull queue — app/ui/models.go's pull endpoint plus app/ui/app/src/screens/models/PullQueueCard.tsx's bounded-parallelism/progress/cancel UI. | app/ui/models_test.go::TestModelsManagerLoadLocked_DownloadingResetToQueuedOnRecovery | partial | An earlier lane added app/ui/models_test.go (5 test functions): repoKey's same-repo grouping, modelsManager.loadLocked's real "downloading is a lie at startup" recovery, and persistLocked+loadLocked's real file round trip. This lane added app/ui/models_pause_resume_cancel_test.go (4 test functions), driving the real pullPause/pullResume/pullCancel HTTP handler methods for the first time: each of the three state-guard rejections (pause a non-downloading job, resume an already-downloading job, cancel an already-completed job) fires the exact production error message before the handler ever touches job.cancel, persists anything, or dispatches; and the one safe state-mutating success path (resuming a paused job flips it to queued, clears its error, and persists that to disk) is proven with every dispatch slot pre-filled so modelsMaxConcurrency alone blocks tryDispatchLocked from reaching a real dispatch. modelsManager() is a package-level singleton bound to the real user's %LOCALAPPDATA%\Ollama\model-pull-queue.json; these tests inject a t.TempDir()-backed manager into that singleton var (confirmed by grep that no other test in the package touches it) rather than ever risk mutating the real file. NOT covered: tryDispatchLocked's bounded-concurrency/same-repo-serialization dispatch loop and runJob itself, both of which talk to a real local Ollama API client — driving them for real would need either a real running server (unavailable here) or mocking the Ollama client, out of this lane's bounded scope; also the SSE fan-out (pullEvents) and PullQueueCard.tsx's own UI. |
| streaming-chat | Streaming chat | npx vitest run src/util/jsonl-parsing.test.ts | Rendering-safety of streamed markdown content, plus (added this lane) the real NDJSON line-reassembly transport (src/util/jsonl-parsing.ts) that api.ts's chat streaming (parseJsonlFromResponse, consumed by useChats.ts) is built on. | app/ui/app/src/util/jsonl-parsing.test.ts::reassembles a single JSON line whose bytes arrive split across the '{' ... '}' boundary of the object itself | partial | Added in this lane (app/ui/app/src/util/jsonl-parsing.test.ts, 4 tests), against real streaming failure modes a happy-path test never exercises: a JSON object split mid-token across two stream chunks correctly reassembles into one value rather than two failed parses; a multi-byte UTF-8 character (é, an emoji, and a CJK character) whose raw bytes are split across a chunk boundary decodes correctly, proving TextDecoder's `{ stream: true }` mode is actually doing its job; a malformed line does not abort the whole stream — the well-formed lines before and after it still yield; and a final line with no trailing newline is still parsed once the stream reports done. Still NOT covered: useChats.ts and StreamingContext.tsx's own hook-level state accumulation as chat events arrive, and the chat-session persistence layer streaming drives — TestChatDeletionWithCascade (cited under integration) exercises chat/message persistence but not streaming itself. StreamingMarkdownContent.test.tsx's own rendering-safety coverage (never enables raw HTML parsing, never renders an attacker-controlled markdown image src) is unchanged and still real. |
| file-conversion | File conversion | go test ./app/ui/... -run TestValidateOutput_RejectsCorruptStructuredResult | The converter's output-validation re-decode gate (validateOutput) for the structured and images categories, plus the picker-issued-path security gate (convertManager.pathIsPicked/registerPickedPaths) every probe/job request must pass before touching a filesystem path at all. | app/ui/convert_test.go::TestValidateOutput_RejectsCorruptStructuredResult | covered | Added in this lane (5 test functions in convert_test.go: two structured-data cases, one image case, and two picker-path-gate cases including TTL expiry — TestValidateOutput_AcceptsWellFormedStructuredResult, TestValidateOutput_RejectsCorruptImageResult, TestConvertManager_RefusesPathThePickerNeverIssued, TestConvertManager_ExpiredPickedPathIsRefusedAndPruned). Not covered: the real conversion adapters themselves (convertText/convertBinary/convertImage/convertArchive), job dispatch/cancel/SSE fan-out, and every external-tool (pandoc/ffmpeg/libavif/libheif) adapter path. |
| pdf-operations | PDF operations | go test ./app/ui/... -run TestConvertPDFToText_ExtractsRealTextFromAMinimalRealPDF | convertPDFToText (text extraction) — the one PDF operation this build currently ships; split/merge/extract/reorder/rotate/metadata are not yet implemented for this project per docs/features/uh-completeness/articles/file-converter.md. | app/ui/convert_pdf_test.go::TestConvertPDFToText_ExtractsRealTextFromAMinimalRealPDF | covered | Added in this lane (app/ui/convert_pdf_test.go, 3 test functions). buildMinimalTextPDF hand-builds a byte-correct single-page PDF (real object offsets, a real cross-reference table, a real trailer) with one Tj text-showing operator, fed through the real github.com/ledongthuc/pdf reader convertPDFToText itself uses — never a mocked reader. Covers real text extraction, rejection of a file that is not a PDF at all, and a structurally valid page with no content stream producing empty output rather than an error (per convertPDFToText's own "a single unreadable page does not fail the whole document" comment). |
| queue-recovery | Queue recovery | go test ./app/ui/... -run TestConvertManagerLoadLocked_OrphanedRunningJobResetToQueuedAndTempFileRemoved | convertManager.loadLocked's startup recovery of a job left running when the process exited — orphaned temp file removal plus reset to queued, the resumable-at-job-granularity contract ConvertJob.TempOutputPath's own doc comment describes. | app/ui/convert_queue_restart_test.go::TestConvertManagerLoadLocked_OrphanedRunningJobResetToQueuedAndTempFileRemoved | covered | Added in this lane (app/ui/convert_queue_restart_test.go, 5 test functions). Writes a real queue file with a job left "running" and a real orphaned temp file on disk, constructs a fresh convertManager against that path, and proves loadLocked resets the job to queued with TempOutputPath cleared and the orphaned temp file actually removed from disk — plus the untouched-terminal-states, no-temp-file, per-instance-idempotent, and real persistLocked/loadLocked file-round-trip contrasting cases. |
| ollama-manager | Ollama suite manager (runtime detection) | go test ./app/ui/... -run TestBuildHardwareResponse_NeverFailsWholeResponseOnOneBadSubProbe | The runtime health/detection layer underneath the whole suite manager — buildHardwareResponse/cachedHardware/getHardware (scraping the real ollama serve log for compute/VRAM facts) and the missing/stopped/unhealthy state handling the Ollama suite manager canonical feature requires. | app/ui/hardware_test.go::TestBuildHardwareResponse_NeverFailsWholeResponseOnOneBadSubProbe | partial | An earlier lane added app/ui/hardware_test.go (6 test functions): buildHardwareResponse is exercised for real (real discover.GetCPUMem syscall, real server.GetInferenceInfo log scrape, real free-disk measurement, real OLLAMA_* env overrides) and proven to always return a non-nil Devices slice plus every response Warning backed by a real logger.Warn call, never failing the whole response on one bad sub-probe; a canceled context degrades within 5s rather than hanging; vramTotals, parseLogBytes and existingDirFor are covered directly. This lane closed the first of that row's own named gaps: app/ui/hardware_cache_test.go proves cachedHardware's 60s TTL for real — a cache primed with a synthetic response and a fresh timestamp is returned by pointer identity unchanged (no rebuild) while within TTL, and aging that same cache past hardwareCacheTTL makes the next call rebuild for real (a genuine buildHardwareResponse call) and refresh the cached timestamp; hardwareCacheResp/hardwareCacheAt/hardwareCacheMu are package-level vars, so the test saves and restores their real state around itself. NOT covered: a genuine GPU-present hardware environment (this test machine's actual devices, whatever they are, decide that branch), and the in-app troubleshooter this feature also requires, which does not exist yet (see guided-recovery). |
| harness-launch | Harness launch | go test ./app/ui/... -run TestLaunchIntegrations_OnlyListsTheAllowedIntersection | app/ui/launch.go's harness-profile launch flow — allowlisted executable/args/env, preflight preview, launch, health verification — plus LaunchIntegrationCard.tsx/CodexHarness.tsx's UI. | app/ui/launch_test.go::TestLaunchIntegrations_OnlyListsTheAllowedIntersection | partial | Added in this lane (app/ui/launch_test.go, 6 test functions): GET /api/v1/launch/integrations only ever lists the real cmd/launch-registry ∩ database.go-allow-list intersection (proven against "cline", a real registered integration this screen must exclude); POST /api/v1/launch/run rejects the wrong HTTP method, a syntactically malformed slug, an unknown integration, and a registered-but-excluded integration ("cline" again, at the run endpoint this time) before ever reaching an installed-binary check; resolveGUIHomeView resolves both a canonical name and a database.go-only alias spelling ("chatgpt" -> "codex-app"). Deliberately NOT exercised: spawnLaunchTerminal and the CheckInstalled()==true success path -- on Windows that shells out to `cmd.exe /c start`, which opens a real, visible console window (confirmed live in this lane: an early draft of this test accidentally reached that path via a "Codex" slug that launchRun's own `strings.ToLower` normalizes to the real "codex" integration before the regex check, and it really did spawn a second copy of this test binary via `start` before self-terminating) -- exactly the side effect a headless test run must never risk causing. |
| configuration-rollback | Configuration rollback | go test ./app/ui/... -run TestCodexManagerRun_RollbackOnFailureRestoresProfileSnapshot | The Codex harness's real snapshot-before-run, automatic-rollback-on-failure contract (app/ui/codex.go): codexManager.start() captures the profile as session.snapshot at the moment a run begins, and codexManager.run() calls restoreProfile(session.snapshot) when the run exits non-zero (or otherwise fails) and the caller opted into rollbackOnFailure. | app/ui/codex_rollback_test.go::TestCodexManagerRun_RollbackOnFailureRestoresProfileSnapshot | covered | Added in this lane (app/ui/codex_rollback_test.go, 4 test functions). Drives codexManager.run() directly against a real subprocess that genuinely exits non-zero (`cmd.exe /c exit 3`) plus a real on-disk profile-history file: proves a profile mutated in memory AND on disk after its pre-run snapshot was captured is reverted to that exact snapshot, in memory and on disk, once the run fails with rollbackOnFailure=true (TestCodexManagerRun_RollbackOnFailureRestoresProfileSnapshot); proves the rollback is conditional on that flag rather than firing on every failure, by leaving the same mutation untouched with the flag off (TestCodexManagerRun_FailureWithoutRollbackFlagLeavesProfileMutated); and proves restoreProfile is a safe no-op when the snapshotted profile ID no longer exists, e.g. deleted mid-run (TestCodexManagerRestoreProfile_UnknownProfileIDIsANoOp). This row was previously reported "missing" on the strength of a grep across only app/ui/launch.go and app/ui/config_profiles.go, which really do have no such contract — that grep never reached app/ui/codex.go, where the Codex harness implements it in full. The separate Launch Integrations harness (launch.go, see the harness-launch row) has no persisted per-run profile of its own to roll back — it launches external tools directly rather than mutating any app-managed configuration — so this contract lives entirely in the Codex harness. |
| guided-recovery | Guided recovery | npx vitest run src/components/recovery | app/ui/app/src/components/recovery/ -- a reusable RecoveryNotice presentational component (state name, plain-language explanation, an honest Reason/NextStep straight from the server, a real Retry, and an optional distinct next-action control) plus five state-specific hooks/notices wired against real, already-registered backend routes that had zero frontend consumer before this lane: Ollama runtime health (GET /api/version, proxied straight to the real Ollama server), no-GPU-detected-yet (GET /api/v1/hardware's empty Devices array), Docker/GPU container passthrough (GET /api/v1/docker/status, POST /api/v1/docker/probe-gpu), model-catalog completeness (GET /api/v1/models/catalog/status, POST /api/v1/models/catalog/refresh), and the pull-queue disk-space preflight refusal (POST /api/v1/models/pull). Wired into both the Models screen (Ollama health, no-GPU, catalog, disk/pull) and the Status screen (Docker/GPU), per the brief's "at minimum" requirement. | app/ui/app/src/components/recovery/useOllamaHealthRecovery.dom.test.tsx::renders the notice for a failed fetch, and Retry re-issues the request | covered | Added this lane (6 *.dom.test.tsx files, 19 tests, all real fetch-mock-backed end-to-end runs through the actual hooks and components -- never a stubbed dependency standing in for the seam under test). Directly proves the brief's three required assertions: RecoveryNotice.dom.test.tsx exercises the presentational contract alone (state/title/explanation/reason/nextStep rendering, a real onRetry call on click, a real distinct action control, no link anywhere in the notice, alert-vs-status role by severity); useOllamaHealthRecovery.dom.test.tsx proves a genuinely failed GET /api/version renders the notice and that clicking Retry issues a second real fetch to the same endpoint, clearing the notice once that second call succeeds; NoGpuNotice.dom.test.tsx proves the exact wording contract by asserting the rendered text contains "not detected yet" and does NOT contain "no gpu" anywhere (case-insensitively, including inside a would-be clarifying negation), plus the undefined-vs-empty-array distinction and a real GET /api/v1/hardware retry. CatalogRecoveryNotice.dom.test.tsx and DockerGpuNotice.dom.test.tsx additionally prove the two backend-supplied Reason/NextStep fields render verbatim (never re-authored parallel copy) and that their action buttons ("Refresh catalog", "Probe GPU passthrough") issue the real POST and fold a real response back in. PullRecoveryNotice.dom.test.tsx proves the disk-preflight scenario end to end through a real usePullRecovery-driven harness: models.go's exact refusal shape ("needs at least 512.0 MB free; only 88.4 MB free on ...") renders verbatim, Retry re-POSTs the identical model, and a non-disk refusal gets the generic (not disk-specific) copy. NOT covered by this suite: the real Docker daemon / real Ollama subprocess behind these endpoints (that belongs to the ollama-manager/unit rows' Go-side tests, which this lane's allowed paths do not touch); a platform-specific "here's how to install/start Ollama or Docker" deep link (this lane renders the server's own Reason/NextStep and a working Retry/action, per the brief's explicit hard rule against inventing parallel guidance, but does not add an install-flow beyond that); and real built-artifact capture evidence (none was requested or taken this pass). |
| accessibility | Accessibility | npx vitest run | Accessible-name/role/state assertions embedded inside individual DOM tests (getByLabelText, getByRole, aria-pressed/aria-invalid/aria-live checks), plus four dedicated, directly-tested accessibility primitives added in an earlier lane: a11yFocusTrap, a11yLayoutAudit, a11yRovingTabindex, and a11yScrollRegion (app/ui/app/src/uh/), and (added this lane) real keyboard-operation coverage of an actual modal overlay rather than only its reusable primitive. | app/ui/app/src/components/md3/ConfirmDialog.dom.test.tsx::moves focus inside the dialog on open, closes on Escape without confirming, and returns focus to the opener | partial | An earlier lane added 27 tests across the four a11y*.dom.test.tsx files proving the reusable focus-trap/layout-audit/roving-tabindex/scroll-region PRIMITIVES work in isolation. This lane closed the gap those left open — a real overlay's actual keyboard behavior — by driving ConfirmDialog (a real Headless UI alertdialog gating a destructive action) from a real opener button: opening it moves focus off the trigger and into the dialog panel, Escape closes it WITHOUT arming the destructive action, and focus returns to the exact button that opened it. Verified this lane to actually catch a regression, not just pass by construction: temporarily breaking the dialog's onClose wiring made this exact test fail (a real Escape-no-longer-closes timeout), then the source was restored clean. Still not a systematic per-surface scan: @storybook/addon-a11y remains interactive/manual only, no automated violation scanner (e.g. axe-core/vitest-axe) is wired into the npx vitest run suite, and only this one real overlay has dedicated keyboard-operation coverage — the app's other modals/menus/popovers (CommandPalette, LockManager, ExportDialog, and the rest) still rely only on their individual DOM tests' incidental role/label assertions, not an explicit open/Escape/focus-return contract each. |
| localization | Localization | npx vitest run src/uh/localization.dom.test.tsx | The three language modes (English/Cantonese/bilingual), both funny-level sliders, and School mode — app/ui/app/src/uh/* (t.ts, funny.ts, localized.ts, school.ts, vocab.ts, provider.tsx) and the 11 *.dict.ts files across the UI. | app/ui/app/src/uh/localization.dom.test.tsx::renders 'english · cantonese' in bilingual (both) mode | covered | Added in this lane (app/ui/app/src/uh/localization.dom.test.tsx, 19 tests), exercised through the real exported public surface (UhProvider/useT/useUh/useShows/funny/applyVocab/fact), writing the exact JSON shape and dispatching the exact PREFERENCES_CHANGED_EVENT a real settings write would, never mocking localStorage or reimplementing the logic under test: all three language modes including a live no-reload preference change and an unrecognized-value fallback to English; School mode forcing langMode/funnyEn/funnyYue/emoji back to their off values even when the stored preferences say otherwise, hiding all four gated feature families (cantonese/humour/dimsum/vocab) while on and showing them while off, and failing closed to "off" for a malformed `school` value that is not the `{ on: true }` shape; funny()'s per-level (0-4) per-language (en/yue) suffix tables and the emoji-at-level-2/escalated-emoji-at-level-4 rule; applyVocab()'s literal (non-regex) find/replace, empty-rules and empty-find no-ops, and later-rule-acts-on-earlier-rule's-output ordering; and fact()'s pass-through for both string and numeric input. NOT covered: narration.ts (TTS, out of this row's own stated Scope), the settings UI that actually writes PREFERENCES_STORAGE_KEY, the scheduled-settings/external-API preference source, and the other 10 *.dict.ts files beyond app.dict.ts's one sampled key (newChat) -- the dictionary lookup MECHANISM (t.ts/getDict) is proven, not every dictionary's own content. |
| security | Security | go test ./app/... -run TestAuthenticationMiddleware | Auth-cookie/token validation, CORS preflight bypass, path-traversal-safe filenames on downloaded updates, unsigned-Windows-installer rejection, direct-URL/tool-argument injection guards, secrets-redaction on export, and (added this lane) the file converter's picker-issued-path allowlist. | app/ui/ui_test.go::TestAuthenticationMiddleware | partial | Real security-relevant coverage is substantial but scattered with no single command that runs only the security-relevant subset: app/updater/updater_test.go::TestUpdateStagePathRejectsUnsafeFilename, TestDownloadNewReleaseRejectsUnsafeHeaderFilename, TestDownloadNewReleaseDoesNotUseRawETagAsPathComponent; app/updater/updater_windows_test.go::TestVerifyDownloadRejectsUnsignedWindowsInstaller; app/tools/url_policy_test.go::TestDirectURLsFromText_RejectsChangedToolArgument; app/tools/browser_test.go::TestBrowserOpen_RejectsUncachedDirectURL; app/store/migration_test.go::TestExportContainsNoSecrets; and (added this lane) app/ui/convert_test.go::TestConvertManager_RefusesPathThePickerNeverIssued, TestConvertManager_ExpiredPickedPathIsRefusedAndPruned. |
| packaging | Packaging | node --test scripts/test/packaging-installer-config.test.mjs | Real, committed coverage of the checked-in Windows installer SOURCE this project actually ships — app/ollama.iss, the real Inno Setup script scripts/build_windows.ps1 compiles with ISCC.exe to produce OllamaSetup.exe. Corrected in this lane from an earlier Squirrel.Windows/Electron description: this project is a native Go application, not Electron, and packages with Inno Setup, not Squirrel.Windows — there is no Setup.exe/RELEASES/.nupkg triplet anywhere in this repository. | scripts/test/packaging-installer-config.test.mjs::the [Setup] AppId GUID matches the [Code] PerUserUninstallKey GUID used for per-user-to-machine-wide migration detection | partial | Added in this lane (scripts/test/packaging-installer-config.test.mjs, 4 tests): the [Setup] AppId GUID and the [Code] section's hardcoded PerUserUninstallKey GUID (used by InitializeSetup's per-user-to-machine-wide migration detection, per the .iss file's own comment on AppId) are proven to match — verified this lane to actually catch drift by temporarily mutating the committed AppId to a different GUID, watching this test go red, then restoring the file clean; no SignTool/SignedUninstaller/code-signing directive is declared anywhere in the source, honoring the permanent no-signing policy; PrivilegesRequiredOverridesAllowed keeps both "dialog" and "commandline" (the latter is what this project's own silent/unattended /ALLUSERS release tooling needs under /VERYSILENT, per the file's own [Setup] comment); and PrivilegesRequired stays "lowest" so a plain double-click install never elevates by default. NOT covered: verifying the actually-produced installer binary itself (the real Setup.exe's file properties, unsigned status of the produced artifact, correct target commit/version) — that needs a full native build through scripts/build_windows.ps1 (MSVC, CUDA/HIP toolchains, ISCC.exe), which is out of this lane's bounded scope; this only verifies the checked-in .iss source the build compiles, not its output. |
| documentation | Documentation | node scripts/check-docs-bundle.mjs | Cross-checks the staged, go:embed-able copy of every feature article (app/ui/articles/) against its source (docs/features/uh-completeness/articles/) and the inventory's feature list — count equality, exact basename-set equality in both directions, and per-file sha256 equality. | scripts/check-docs-bundle.mjs | covered | Re-measured this session (2026-08-19, final evidence pass): the staged bundle had drifted stale again (a `--fix` from an unrelated, uncommitted lane never landed on this branch), reproduced as 70 stale-sha256 problems, then repaired with `node scripts/check-docs-bundle.mjs --fix` and re-verified: "85 staged article(s) in app/ui/articles/ match 85 inventory feature(s), byte for byte." The fix touches only app/ui/articles/, a mechanical byte-for-byte copy of this lane's own docs/features/uh-completeness/articles/** source with no independent authorship of its own — kept in this pass specifically so the shipped app's own offline docs browser (captured in docs.png) stops claiming up to 70 articles are "Not written" (the docs.png capture in this pass shows several while the bundle was still stale) when their source already carries real, hand-written prose — only the 13 articles matching this inventory's 13 genuinely `missing` feature rows are scaffold end to end. |
| completeness | Completeness (UH inventory) | node scripts/check-uh-inventory.mjs --self-test && node scripts/check-uh-inventory.mjs | Structural validation of the shared feature-completeness inventory (docs/features/uh-completeness/inventory.json) — every canonical feature ID present exactly once, every non-null evidence field format-valid and filesystem-resolved, plus a self-test that removes the exact language-modes row and asserts red, then restores and asserts green. | scripts/check-uh-inventory.mjs | covered | Verified this session: --self-test reports "28 guard cases exercised... all 26 declared guard codes were observed failing at least once", and the structural check on the real committed inventory passes clean. |
| capture-coverage | Capture coverage | node scripts/check-uh-inventory.mjs --self-test | Whether a feature row's claimed real-capture evidence (a screenshot tied to the exact build commit) actually resolves — check-uh-inventory.mjs's resolveCaptureEvidence re-hashes the claimed file and compares against the recorded sha256, refusing a hash mismatch, a missing file, or a malformed path@sha256:hex claim. | scripts/check-uh-inventory.mjs | partial | The verification mechanism is real and proven (its CAPTURE_FORMAT_INVALID/CAPTURE_FILE_NOT_FOUND/CAPTURE_HASH_MISMATCH guard cases are three of the 26 the self-test above watches turn red and back to green). Actual coverage as re-measured this session (2026-08-19, final evidence pass, after merging a real recapture pass that fixed the /settings crash and re-shot all 12 screenshots against it): 26/170 desktop-app+landing-page evidence objects now carry a captureEvidence value that genuinely resolves against real image bytes, up from 24/170 -- still landing-page-only zero (all 85 landing-page rows remain "missing"; this project has no landing-site work in this pass) and desktop-app is the only surface with any real captures. The former settings.png crash capture is gone: settings.png now shows the real Settings screen's General card, and two more rows (dialog-emoji-toggle, settings-explanations-provenance) were wired to it this pass on the strength of that real render. A real CDP network audit was also added this pass (docs/features/uh-completeness/captures/manifest.json#networkAudit): 182 requests recorded across the 9 base screens, 178 unique, 0 non-loopback -- the evidence behind the `no-network-privacy` row, though that row itself stays `missing` because its other six evidence fields (implementation, documentation, localizedCopy, persistence, focusedCheck) still have no committed implementation on this branch. |

## Checks

The structural check (every required area present exactly once; every `covered`/`partial` row's
evidence resolves; every `missing` row carries a real reason) runs with:

```
node scripts/check-suite-inventory.mjs
```

The negative-regression self-test mutates one row (or the table itself) at a time — a required area
removed, a duplicate or unrecognized area id, an invalid status, a `missing` row that keeps a real
command or evidence claim, a `missing`/`partial` row whose reason is too short to be real, a
`covered` row with an empty command or a placeholder evidence claim, a malformed evidence string, an
evidence path that escapes the repository, an evidence path that does not exist, a claimed test
renamed to a superstring of itself, and a claimed test whose line still exists but is commented out
— and requires every one of those mutations to turn the checker red with its own exact guard code,
then requires the checker to go green again once the fixture is restored:

```
node scripts/check-suite-inventory.mjs --self-test
```

Both are wired into the root `package.json` as `npm run verify:suites`.
