# Issue Handoff

## Behaviour

Filing a handoff comment on a GitHub issue is a repository-maintenance action taken by whoever is doing the work, not a feature the desktop app renders for its end users. `HANDOFF.md` at the repository root is the real, current artifact for this: it is kept accurate on every project-changing task and is where a next owner (human or agent) is meant to start.

## Configuration

TODO(issue-handoff): describe how a user or operator configures this feature -- the settings surface, its defaults, and where the choice persists.

## Failure modes

TODO(issue-handoff): describe what happens when this feature cannot do its job -- a missing dependency, offline operation, invalid input -- and what the user sees.

## Security considerations

TODO(issue-handoff): describe what this feature must never expose or allow, and the exact mechanism that enforces it.

## Verification

TODO(issue-handoff): name the focused test(s), the built-artifact interaction proof, and the real capture evidence that back this feature.

## Suggested articles

TODO(issue-handoff): link the related features, the prerequisites, and the natural next article a reader should open.
